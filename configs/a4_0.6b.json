{
  "_comment": "A4 dry run on Qwen3-0.6B, 3000 steps, same settings as a4_8b.json otherwise. Proves the A4 path on a cheap model before the 8B run. Expect the parse gate to fire: 0.6B produced valid JSON on 8% of replies on the linear task.",
  "extends": "slice_plunkett_0.6b.json",
  "decision_rule": {
    "type": "tradeoff",
    "active_cuts": 1,
    "cut_range": [
      30,
      70
    ],
    "zero_cut_main_effects": true
  },
  "model_training": {
    "data_dir": "data/a4/"
  },
  "model_estimating": {
    "type": "tradeoff",
    "estimation_trials_per_persona": 200
  },
  "report_schema": {
    "max_new_tokens": 400
  },
  "model_hyperparameters": {
    "training_steps": 3000,
    "checkpoint_every": 500,
    "introspection_steps": 300
  }
}
