{
  "_comment": "Proves the CUDA path through both stages on Qwen3-0.6B in sixty steps, gates off. Not a result.",
  "extends": "default.json",
  "model_training": {
    "instances": 10
  },
  "model_estimating": {
    "estimation_trials_per_persona": 20,
    "chance_draws": 200
  },
  "report_schema": {
    "samples_per_report": 3,
    "max_new_tokens": 96
  },
  "model_hyperparameters": {
    "quantization": "none",
    "batch_size": 4,
    "training_steps": 60,
    "checkpoint_every": 30,
    "introspection_steps": 10,
    "gradient_checkpointing": false
  },
  "gates": {
    "min_decision_accuracy": 0.0,
    "min_parse_rate": 0.0
  },
  "compute": {
    "backend": "local",
    "mixed_precision": "bf16"
  }
}
